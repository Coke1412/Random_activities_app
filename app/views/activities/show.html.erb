<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @activity.name %>
</p>

<% if @activity.image.attached? %>
<p>
  <strong>Image:</strong>
  <br>
    <% @activity.image.each do |image| %>
    <%= image_tag image.variant(resize:"400x400") %> <br> <br>
    <%= link_to 'Remove', delete_image_category_path(@ativity, image), method: :delete, data: { confirm: 'Are you sure?' } %>
  <% end %>
</p>
<% end %>


<p>
  <strong>Category:</strong>
  <%= @activity.category %>
</p>

<%= link_to 'Edit', edit_activity_path(@activity) %> |
<%= link_to 'Back', activities_path %>
